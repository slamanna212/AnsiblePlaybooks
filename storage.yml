#################################################
# NFS Server backed by DO Volume
#################################################
---
    - hosts: all
      become: true
      vars_files:
        - group_vars/storage.yml

    # Install Packages
        - name: Update package mirrors and existing packages
          apt: update_cache=yes upgrade=yes
    
        - name: Install required packages
          apt: name={{ sys_packages }} state=latest

    # Install Packages